Sources for the plugin `plugin.appleSignIn`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appleSignIn" = {
            publisherId = "tech.scotth",
        },
    },
}
```
